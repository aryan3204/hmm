# 🤖 hmm - Voice Activated AstroBrain

Say "hmm" → wakes up → suggests BEST action (astrology + memory + patterns)

## 🚀 Quick Start
```bash
pip install -r requirements.txt
cp .env.example .env
python main.py
**→ Commit with message: "Add README"**

**File 2: `requirements.txt`** (same button)
**→ Commit: "Add requirements"**

**File 3: `.env.example`**
**→ Commit: "Add env template"**

**File 4: `.gitignore`**
**→ Commit: "Add gitignore"**

**File 5: `main.py`** (copy from my previous response - the long one)
**→ Commit: "Add main voice activation"**

### **3. Create Folders + Files (use `/` trick)**

**Click "Add file" → "Create new file"** and type folder name + `/filename`:

**`brain/__init__.py`** (empty file) → Commit
**`brain/agent_core.py`** → paste code → Commit  
**`brain/memory_manager.py`** → paste → Commit
**`brain/insight_engine.py`** → paste → Commit
**`brain/astro_brain.py`** → paste → Commit

**`prompts/hmm_wake_prompt.txt`** → paste prompt → Commit
**`prompts/system_prompt.txt`** → paste → Commit

**`data/planets.json`** → paste JSON → Commit

### **4. Verify Structure**
Your repo should show exactly:
**→ Commit: "Add gitignore"**

**File 5: `main.py`** (copy from my previous response - the long one)
**→ Commit: "Add main voice activation"**

### **3. Create Folders + Files (use `/` trick)**

**Click "Add file" → "Create new file"** and type folder name + `/filename`:

**`brain/__init__.py`** (empty file) → Commit
**`brain/agent_core.py`** → paste code → Commit  
**`brain/memory_manager.py`** → paste → Commit
**`brain/insight_engine.py`** → paste → Commit
**`brain/astro_brain.py`** → paste → Commit

**`prompts/hmm_wake_prompt.txt`** → paste prompt → Commit
**`prompts/system_prompt.txt`** → paste → Commit

**`data/planets.json`** → paste JSON → Commit

### **4. Verify Structure**
Your repo should show exactly:

### **5. TEST IT**

## 🚀 **Pro Tip: GitHub Template**
After it's working → **"Use this template"** button → reuse for future projects!

**Start with README.md NOW** → screenshot after 3 files → I'll guide next batch! 

**No VS Code. No terminal. Pure GitHub web = 10x faster for prototypes.** 🎉
